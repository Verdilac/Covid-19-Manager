import React from "react"
import ReactDOM from "react-dom"
import ReactRout from "./components/ReactRout"
import "bootstrap/dist/css/bootstrap.min.css"

ReactDOM.render(
  <React.StrictMode>
    <ReactRout />
  </React.StrictMode>,
  document.getElementById("root")
)
